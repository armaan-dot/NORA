"""nora_core.affordance — affordance scoring sub-package.

Provides:
- :mod:`.scoring`  — :class:`AffordanceScore` dataclass and :class:`BaseScorer` ABC
- :mod:`.fusion`   — :class:`WeightedProductFusion` for combining sub-scores
- :mod:`.metrics`  — :class:`AffordanceMetricsLogger` for CSV logging
"""
